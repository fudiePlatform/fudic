/**
 * Placeholder entry point for the @fudic scope.
 */

/** Package name, useful as a smoke test that the module loads. */
export const name = "@fudic/core";

/** Current package version. */
export const version = "0.0.1";