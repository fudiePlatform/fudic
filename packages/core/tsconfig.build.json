{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    // The build resolves @fudic/dom via its published types (node_modules → dist),
    // built first by the topological `pnpm -r build`. Source `paths` is a dev-only
    // convenience for typecheck/tests, not for emit.
    "paths": {}
  },
  "include": ["src"]
}
