@code {
  // inside @code the comment is JavaScript's own (BUG-13): @* … *@ is FUD0114 here
  const tone = 'neutral';
}

<app-comments>
  <template shadowrootmode="open">
    @* before the construct *@
    @if (tone === 'neutral') {
      <p>neutral</p>
    } @* between the brace and the else *@ else if (tone === 'warm') {
      <p>warm</p>
    } @* and before the final else *@ else {
      <p>other</p>
    }

    @switch (tone) { @* before the first label *@
      case 'neutral':
        <b>n</b>
      @* between two labels *@
      default:
        <b>d</b>
      @* after the last one *@
    }
  </template>
</app-comments>
