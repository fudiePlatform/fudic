{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fudic/core": "{{version}}",
    "@fudic/ssr": "{{version}}",
    "@fudic/transport": "{{version}}"
  },
  "devDependencies": {
    "@fudic/vite": "{{version}}",
    "typescript": "{{tsVersion}}",
    "vite": "{{viteVersion}}"
  }
}
